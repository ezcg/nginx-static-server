Requires docker installed along with docker-compose

First time running, in nginx-static-server run:

docker-compose up --build

Thereafter, just do:

docker-compose up

Open http://localhost:8080 in your browser.